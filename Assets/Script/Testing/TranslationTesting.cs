using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TranslationTesting : MonoBehaviour
{
    //public bool move;

    //public Vector3 vA;
    //public Vector3 vB;
    //public Vector3 vC;

    //public Vector3 vD;

    //void Update()
    //{
    //    if (move)
    //    {
    //        move = false;

    //        Vector3 forward = 

    //        Translate();
    //    }
    //}

    //void Translate()
    //{
    //    Mesh mesh = this.gameObject.GetComponent<MeshFilter>().mesh;

    //    mesh.Clear();
    //    mesh.vertices = new Vector3[] { vA, vB, vC, vD };
    //    mesh.triangles = new int[] { 0, 1, 2,
    //                                 1, 3, 2 };
    //    mesh.Optimize();
    //    mesh.RecalculateNormals();

    //    MeshCollider meshCol = this.gameObject.GetComponent<MeshCollider>();
    //    meshCol.sharedMesh = new Mesh();
    //    meshCol.sharedMesh.vertices = new Vector3[] { vA, vB, vC, vD };
    //    meshCol.sharedMesh.triangles = new int[] { 0, 1, 2,
    //                                               1, 3, 2 };
    //    meshCol.sharedMesh.RecalculateBounds();
    //}

    //Vector3 RelativeMovement(Vector3 origin, Vector3 p, Vector3 forward, Vector3 up, Vector3 right)
    //{
    //    return new Vector3((p.x * right.x) + (p.y * up.x) + (p.z * forward.x) + origin.x, (p.x * right.y) + (p.y * up.y) + (p.z * forward.y) + origin.y, (p.x * right.z) + (p.y * up.z) + (p.z * forward.z) + origin.z);
    //}
}
